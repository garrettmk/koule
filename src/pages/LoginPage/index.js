import React from 'react';
import MessageButton from "../../components/MessageButton";

export default function LoginPage() {
  return (
    <div>
      Login
      <MessageButton message={'SIGN_IN'}>Sign In</MessageButton>
    </div>
  );
}